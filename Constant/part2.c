#include <stdio.h>
//using constant with the const keyword


int main(void)
{
    // when use the const keyword it become in a read only var. can not change value.
    const int var = 67;
    return 0;
}