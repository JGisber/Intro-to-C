#include <stdio.h>

int main(void)
{
    int var = 0x43FF; // hexadecimal number
    printf("%d\n", var); // the format specifier says to print the hex value
    return 0;            // if %d then it would print the decimal value
}